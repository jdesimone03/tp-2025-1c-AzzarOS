{
    "ip_cpu": "${IP_CPU}",
    "port_cpu": "${PORT_CPU}",

    "ip_memory": "${IP_MEMORY}",
    "port_memory": "${PORT_MEMORY}",

    "ip_kernel": "${IP_KERNEL}",
    "port_kernel": "${PORT_KERNEL}",

    "tlb_entries": 15,
    "tlb_replacement": "TLB",
    "cache_entries": 10,
    "cache_replacement": "CLOCK",
    "cache_delay": 10,

    "log_level": "${LOG_LEVEL_CPU}"
}

{
    "ip_cpu": "${IP_CPU}",
    "port_cpu": "${PORT_CPU}",

    "ip_memory": "${IP_MEMORY}",
    "port_memory": "${PORT_MEMORY}",

    "ip_kernel": "${IP_KERNEL}",
    "port_kernel": "${PORT_KERNEL}",

    "tlb_entries": 4,
    "tlb_replacement": "LRU",
    "cache_entries": 2,
    "cache_replacement": "CLOCK-M",
    "cache_delay": 250,

    "log_level": "${LOG_LEVEL_CPU}"
}
